<?xml version="1.0" encoding="utf-8"?>
<layout>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

        <ImageView
            android:id="@+id/playlist_art"
            android:layout_width="50dp"
            android:layout_height="50dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toLeftOf="@id/playlists_name"
            android:src="@drawable/ic_baseline_folder_24"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/playlists_name"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_margin="15dp"
            android:gravity="center_vertical"
            android:singleLine="true"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintRight_toLeftOf="@id/playlist_nof"
            app:layout_constraintLeft_toRightOf="@id/playlist_art"
            app:layout_constraintTop_toTopOf="parent"
            android:textSize="18sp"
            android:textColor="#000000"
            tools:text="Dev's playlists"/>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_margin="15dp"
            android:id="@+id/playlist_nof"
            android:gravity="center_vertical"
            android:textColor="#7B7B7B"
            app:layout_constraintLeft_toRightOf="@id/playlists_name"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:text="(3 songs)"/>



    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>